import {
    LOGIN_SUCCESS,
    LOGIN_FAIL,
    LOAD_USER_SUCCESS,
    LOAD_USER_FAIL,
} from '../actions/types'

export const load_user = () => async dispatch => {

}

export const login = (email, password) => async dispatch => {
    
}